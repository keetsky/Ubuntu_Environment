http://ookamiantd.top/2017/build-blog-hexo-advanced/#好玩的样式
http://ookamiantd.top/2017/build-blog-hexo-advanced/#%E5%8D%9A%E5%AE%A2%E6%9B%B4%E6%8D%A2Disqus%E8%AF%84%E8%AE%BA
https://reuixiy.github.io/technology/computer/computer-aided-art/2017/06/09/hexo-next-optimization.html

要想NexT主题的简体中文字体配置生效，站点配置文件中的language必须为zh-Hans。




主题字体配置

font:
  enable: true
  # Uri of fonts host. E.g. //fonts.googleapis.com (Default)
  # 亲测这个可用，如果不可用，自己搜索：Google字体 国内镜像，找个能用的就行
  host: https://fonts.proxy.ustclug.org
  # Global font settings used on <body> element.
  global:
    # external: true will load this font family from host.
    external: true
    family: Lato
  # Font settings for Headlines (h1, h2, h3, h4, h5, h6)
  # Fallback to `global` font settings.
  headings:
    external: true
    family:
  # Font settings for posts
  # Fallback to `global` font settings.
  posts:
    external: true
    family:
  # Font settings for Logo
  # Fallback to `global` font settings.
  # The `size` option use `px` as unit
  logo:
    external: true
    family: Lato
    size:
  # Font settings for <code> and code blocks.
  codes:
    external: true
    family: "Source Code Pro"
    size:
